#include <stdio.h>
int num[1000];
int keep[505][505];
int MAX(int x) {
    if (x == 1) return 1;
    return n*MAX(n -1);    
}
int main() {
    int x;
    scanf("%d", &x);
    y = MAX(x);
    while( y > 0) {
       for (int i = 0; i <= 100 ; i++) {
            num[i] = y % 10;
            y /= 10;
        }
    }     
    for (int j = 0; num[j] = 0; j++) {
        ans = num    
    }
    printf("%d",
    return 0;
}






