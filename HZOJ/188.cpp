#include <iostream>

using namespace std;
int prime[1000];
 
int main() {
    int m, n;
    cin >> m >> n;
    int x, y;
    y = m > n ? m : n;
    x = m < n ? m : n;
    for (i = x; i <= y; i++) {
        if (prime[i]) continue;
        
    }
}
